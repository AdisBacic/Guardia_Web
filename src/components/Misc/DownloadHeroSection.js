import React from 'react';

const DownloadHeroSection = () => {
  return (
    <div className="text-center py-20 bg-gray-800">
       <h1 className="mb-4 text-3xl font-extrabold text-gray-900 text-white md:text-5xl lg:text-6xl">
        Download <span className="text-transparent bg-clip-text bg-gradient-to-r to-emerald-600 from-sky-400">Guardia</span>
      </h1>
      <p className="mx-auto max-w-2xl text-lg font-medium text-gray-400">
       Upplev enkel och effektiv artikelhantering. Tillgänglig för Windows, Mac (Intel) och Mac (ARM64).
      </p>
    </div>
  );
};

export default DownloadHeroSection;
